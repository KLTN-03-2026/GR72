import 'dotenv/config';

async function bootstrap() {
  // Seed data will be rebuilt with the new package-first schema.
  console.log('No seed data configured for the cleaned backend yet.');
}

void bootstrap();

